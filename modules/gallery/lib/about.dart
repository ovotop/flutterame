import 'package:flutter/material.dart';

class GalleryAbout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("about gallery"),
      ),
      body: Center(
        child: Text("about gallery"),
      ),
    );
  }
}
